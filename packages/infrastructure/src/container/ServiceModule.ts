import { ContainerModule, type ContainerModuleLoadOptions } from "inversify";

export const serviceModule = new ContainerModule(
	(options: ContainerModuleLoadOptions) => {
		// Service bindings - add as needed
	},
);
