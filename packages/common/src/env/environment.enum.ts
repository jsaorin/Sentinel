export enum Environments {
	PRODUCTION = "production",
	DEV = "development",
	TEST = "test",
	LOCAL = "local",
}

export enum EnvironmentFile {
	DEFAULT = ".env",
	PRODUCTION = ".env.prod",
	DEV = ".env.dev",
	TEST = ".env.test",
	LOCAL = ".env.local",
}
